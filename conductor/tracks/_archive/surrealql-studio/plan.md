# SurrealQL Studio — Plan

To be filled in when Wave 2 of [[surrealql-toolkit]] lands. Skeleton:

## Phase 1 — Metadata + browse
1. [ ] Embed schema introspector behind `/studio/api/schema`.
2. [ ] Per-table paginated row view with avatar scoping.
3. [ ] Graph-traversal navigation (FK-resolved neighbors).

## Phase 2 — Query playground + EXPLAIN
4. [ ] Typed completion against Mermaid schema.
5. [ ] `EXPLAIN` invocation + render.
6. [ ] Query history (in-memory, per-session).

## Phase 3 — MCP integration + ops surface
7. [ ] Expose studio metadata API as MCP tools.
8. [ ] Operator ops view (bridge state, saga queue, dead-letter).
